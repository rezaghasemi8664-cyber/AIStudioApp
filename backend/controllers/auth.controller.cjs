// backend/controllers/auth.controller.cjs - authentication, registration and password recovery
'use strict';

const bcrypt = require('bcryptjs');
const jwt = require('jsonwebtoken');
const crypto = require('crypto');
const prismaModule = require('../config/prisma.cjs');
const prisma = prismaModule.prisma || prismaModule.default || prismaModule;
const { validateEmail, sendPasswordEmail, normalizeEmail } = require('../services/email.service.cjs');
const { encryptPassword, decryptPassword } = require('../services/passwordVault.service.cjs');

let env = {};
try { env = require('../config/env.cjs'); } catch (_) {}

function secret(type) {
  if (type === 'access') return env.JWT_ACCESS_SECRET || process.env.JWT_ACCESS_SECRET || env.JWT_SECRET || process.env.JWT_SECRET || 'fallback-access-secret';
  if (type === 'refresh') return env.JWT_REFRESH_SECRET || process.env.JWT_REFRESH_SECRET || env.JWT_SECRET || process.env.JWT_SECRET || 'fallback-refresh-secret';
  return process.env.JWT_SECRET || 'fallback-secret';
}
function expiry(type) { if (type === 'access') return env.JWT_ACCESS_EXPIRY || process.env.JWT_ACCESS_EXPIRY || '24h'; return env.JWT_REFRESH_EXPIRY || process.env.JWT_REFRESH_EXPIRY || '7d'; }
function getCookieOptions(req) { const host=String(req.headers.host||'').toLowerCase(),local=host.includes('localhost')||host.startsWith('127.0.0.1'),https=req.secure===true||String(req.headers['x-forwarded-proto']||'').toLowerCase()==='https'; const secure=!local&&https; return {httpOnly:true,secure,sameSite:secure?'none':'lax',path:'/'}; }
function setCookies(req,res,a,r) { const o=getCookieOptions(req); res.cookie('accessToken',a,{...o,maxAge:86400000});res.cookie('token',a,{...o,maxAge:86400000});res.cookie('refreshToken',r,{...o,maxAge:604800000}); }
function clearCookies(req,res){const o=getCookieOptions(req);res.clearCookie('accessToken',o);res.clearCookie('token',o);res.clearCookie('refreshToken',o);}
function parseId(v){if(v===undefined||v===null||String(v).trim()==='')return null;return /^\d+$/.test(String(v).trim())?Number(v):String(v).trim();}
function remainingDays(start,end,months){let finish=end?new Date(end):null;if(!finish&&start&&months>0){finish=new Date(start);finish.setMonth(finish.getMonth()+months);}if(!finish||isNaN(finish.getTime()))return 0;return Math.max(0,Math.ceil((finish.getTime()-Date.now())/86400000));}function subscriptionDurationDays(start,end,months){const s=start?new Date(start):null,e=end?new Date(end):null;if(s&&!isNaN(s.getTime())&&e&&!isNaN(e.getTime()))return Math.max(0,Math.ceil((e.getTime()-s.getTime())/86400000));if(s&&!isNaN(s.getTime())&&months>0){const legacyEnd=new Date(s);legacyEnd.setMonth(legacyEnd.getMonth()+months);return Math.max(0,Math.ceil((legacyEnd.getTime()-s.getTime())/86400000));}return 0;}
const USER_SELECT={id:true,username:true,email:true,passwordHash:true,passwordEncrypted:true,passwordChangedAt:true,name:true,firstName:true,lastName:true,phone:true,mobile:true,nationalId:true,bio:true,avatar:true,isActive:true,isDeleted:true,roleId:true,subscriptionStart:true,subscriptionEnd:true,subscriptionMonths:true,subscriptionType:true,analysisLimit:true,analysisLimit24h:true,analysisUsed24h:true,lastAnalysisReset:true,lastLoginAt:true,loginCount:true,createdAt:true,updatedAt:true,Role:{select:{id:true,name:true,title:true}}};
const PUBLIC_SELECT={...USER_SELECT,passwordHash:false,passwordEncrypted:false,passwordChangedAt:false};
function formatUser(u){const role=u.Role||u.role,fn=u.firstName||'',ln=u.lastName||'',days=remainingDays(u.subscriptionStart,u.subscriptionEnd,u.subscriptionMonths);return{id:u.id,username:u.username,email:u.email||'',name:u.name||[fn,ln].filter(Boolean).join(' '),firstName:fn,lastName:ln,phone:u.phone||u.mobile||'',mobile:u.mobile||u.phone||'',nationalId:u.nationalId||'',bio:u.bio||'',avatar:u.avatar||'',isActive:u.isActive,isDeleted:u.isDeleted||false,role:role?.name||'USER',roleName:role?.name||'USER',roleTitle:role?.title||role?.name||'USER',roleId:u.roleId,isAdmin:role?.name==='ADMIN'||role?.name==='SUPERADMIN'||u.roleId===2,subscriptionStart:u.subscriptionStart||null,subscriptionEnd:u.subscriptionEnd||null,subscriptionDays:subscriptionDurationDays(u.subscriptionStart,u.subscriptionEnd,u.subscriptionMonths),subscriptionMonths:u.subscriptionMonths||0,subscriptionType:u.subscriptionType||'free',analysisLimit:u.analysisLimit24h||u.analysisLimit||0,analysisLimit24h:u.analysisLimit24h||u.analysisLimit||0,remainingDays:days,isSubscriptionActive:days>0,lastLoginAt:u.lastLoginAt||null,createdAt:u.createdAt,updatedAt:u.updatedAt};}
function tokens(u){const role=u.Role||u.role,p={userId:u.id,sub:u.id,username:u.username,email:u.email||null,role:role?.name||'USER',isAdmin:role?.name==='ADMIN'||role?.name==='SUPERADMIN'||u.roleId===2};return{accessToken:jwt.sign(p,secret('access'),{expiresIn:expiry('access')}),refreshToken:jwt.sign({userId:u.id,sub:u.id,type:'refresh'},secret('refresh'),{expiresIn:expiry('refresh')})};}
function required(v){return typeof v==='string'&&v.trim()!=='';}
async function login(req,res){try{const id=normalizeEmail(req.body?.email||req.body?.username),password=String(req.body?.password||'');if(!required(id)||!password)return res.status(400).json({success:false,message:'ایمیل و کلمه عبور الزامی است'});const u=await prisma.user.findFirst({where:{OR:[{email:id},{username:id}],isDeleted:false},select:USER_SELECT});if(!u)return res.status(401).json({success:false,message:'ایمیل یا کلمه عبور اشتباه است'});if(!u.isActive)return res.status(403).json({success:false,message:'حساب کاربری غیرفعال است'});if(!(await bcrypt.compare(password,u.passwordHash)))return res.status(401).json({success:false,message:'ایمیل یا کلمه عبور اشتباه است'});const t=tokens(u);setCookies(req,res,t.accessToken,t.refreshToken);await prisma.user.update({where:{id:u.id},data:{lastLoginAt:new Date(),loginCount:{increment:1}}}).catch(()=>{});return res.json({success:true,message:'ورود موفق',data:{user:formatUser({...u,lastLoginAt:new Date()}),token:t.accessToken,accessToken:t.accessToken,refreshToken:t.refreshToken}});}catch(e){console.error('[AUTH] login',e);return res.status(500).json({success:false,message:'خطای سرور'});}}
function generatePassword(){const letters='ABCDEFGHJKLMNPQRSTUVWXYZabcdefghijkmnopqrstuvwxyz',digits='23456789';let out='';for(let i=0;i<8;i++)out+=letters[crypto.randomInt(letters.length)];for(let i=0;i<4;i++)out+=digits[crypto.randomInt(digits.length)];return out.split('').sort(()=>crypto.randomInt(3)-1).join('');}
async function register(req,res){let createdId=null;try{const email=normalizeEmail(req.body?.email),name=String(req.body?.name||[req.body?.firstName,req.body?.lastName].filter(Boolean).join(' ')).trim(),phone=String(req.body?.phone||req.body?.mobile||'').trim()||null;if(!required(email))return res.status(400).json({success:false,message:'ایمیل الزامی است'});const valid=await validateEmail(email);if(!valid.valid)return res.status(400).json({success:false,message:'ایمیل واردشده معتبر نیست'});const existing=await prisma.user.findFirst({where:{OR:[{email},{username:email}]},select:{id:true,email:true,username:true,isDeleted:true}});if(existing)return res.status(409).json({success:false,message:'شما قبلا با این ایمیل ثبت نام انجام داده اید'});const role=await prisma.role.findFirst({where:{name:'USER'}}),password=generatePassword(),passwordHash=await bcrypt.hash(password,12),passwordEncrypted=encryptPassword(password),start=new Date(),end=new Date(start.getTime()+2*86400000);const u=await prisma.user.create({data:{username:email,email,passwordHash,passwordEncrypted,name:name||email,phone,mobile:phone,roleId:role?.id||1,isActive:true,isDeleted:false,subscriptionStart:start,subscriptionEnd:end,subscriptionMonths:0,subscriptionType:'free'},select:USER_SELECT});createdId=u.id;try{await sendPasswordEmail(email,password);}catch(mailError){await prisma.user.delete({where:{id:createdId}}).catch(()=>{});console.error('[AUTH] registration email failed',mailError);return res.status(502).json({success:false,message:'ارسال ایمیل انجام نشد. لطفا تنظیمات ایمیل را بررسی کرده و دوباره تلاش کنید'});}return res.status(201).json({success:true,message:'کلمه عبور به ایمیل شما ارسال گردید لطفا جهت مشاهده کلمه عبور ایمیل خود را بررسی نمایید',data:{user:formatUser(u),emailSent:true}});}catch(e){console.error('[AUTH] register',e);if(e.code==='P2002')return res.status(409).json({success:false,message:'شما قبلا با این ایمیل ثبت نام انجام داده اید'});return res.status(500).json({success:false,message:'خطای سرور در ثبت نام'});}}
async function verify(req,res){try{const auth=req.headers.authorization||'',tok=auth.startsWith('Bearer ')?auth.slice(7):req.cookies?.accessToken||req.cookies?.token;if(!tok)return res.status(401).json({success:false,valid:false,message:'توکن ارائه نشده'});const d=jwt.verify(tok,secret('access')),u=await prisma.user.findUnique({where:{id:parseId(d.userId||d.sub)},select:PUBLIC_SELECT});if(!u||!u.isActive||u.isDeleted)return res.status(401).json({success:false,valid:false,message:'کاربر یافت نشد یا غیرفعال است'});return res.json({success:true,valid:true,data:{user:formatUser(u)}});}catch(e){return res.status(401).json({success:false,valid:false,message:'توکن نامعتبر'});}}
async function me(req,res){try{const id=parseId(req.user?.userId||req.user?.id||req.user?.sub);if(!id)return res.status(401).json({success:false,message:'شناسه کاربر نامعتبر'});const u=await prisma.user.findUnique({where:{id},select:PUBLIC_SELECT});if(!u)return res.status(404).json({success:false,message:'کاربر یافت نشد'});return res.json({success:true,data:{user:formatUser(u)}});}catch(e){return res.status(500).json({success:false,message:'خطای سرور'});}}
async function updateProfile(req,res){try{const id=parseId(req.user?.userId||req.user?.id||req.user?.sub);if(!id)return res.status(401).json({success:false,message:'شناسه کاربر نامعتبر است'});const b=req.body||{},d={};for(const k of ['firstName','lastName','phone','mobile','nationalId','bio','avatar'])if(b[k]!==undefined)d[k]=(typeof b[k]==='string'?b[k].trim():b[k])||null;if(b.name!==undefined)d.name=String(b.name||'').trim()||null;else if(b.firstName!==undefined||b.lastName!==undefined)d.name=[b.firstName,b.lastName].filter(Boolean).join(' ').trim()||null;if(b.email!==undefined)d.email=normalizeEmail(b.email)||null;if(!Object.keys(d).length)return res.status(400).json({success:false,message:'داده‌ای برای به‌روزرسانی ارسال نشده'});const u=await prisma.user.update({where:{id},data:d,select:PUBLIC_SELECT});return res.json({success:true,message:'پروفایل به‌روزرسانی شد',data:{user:formatUser(u)}});}catch(e){return res.status(e.code==='P2002'?409:500).json({success:false,message:e.code==='P2002'?'ایمیل تکراری است':'خطا در به‌روزرسانی پروفایل'});}}
async function changePassword(req,res){try{const id=parseId(req.user?.userId||req.user?.id||req.user?.sub),current=String(req.body?.currentPassword||''),next=String(req.body?.newPassword||'');if(!id)return res.status(401).json({success:false,message:'شناسه کاربر نامعتبر است'});if(current.length<1||next.length<6)return res.status(400).json({success:false,message:'رمز عبور فعلی و کلمه عبور جدید الزامی است و کلمه عبور جدید باید حداقل ۶ کاراکتر باشد'});const u=await prisma.user.findUnique({where:{id},select:{id:true,passwordHash:true}});if(!u||!(await bcrypt.compare(current,u.passwordHash)))return res.status(401).json({success:false,message:'کلمه عبور فعلی اشتباه است'});const hash=await bcrypt.hash(next,12);await prisma.user.update({where:{id},data:{passwordHash:hash,passwordEncrypted:encryptPassword(next),passwordChangedAt:new Date(),updatedAt:new Date()}});await prisma.session.deleteMany({where:{userId:id}}).catch(()=>{});clearCookies(req,res);return res.json({success:true,forceLogout:true,message:'کلمه عبور با موفقیت تغییر کرد'});}catch(e){console.error('[AUTH] changePassword',e);return res.status(500).json({success:false,message:'خطا در تغییر کلمه عبور'});}}
async function getSubscription(req,res){try{const id=parseId(req.user?.userId||req.user?.id||req.user?.sub),u=await prisma.user.findUnique({where:{id},select:{subscriptionStart:true,subscriptionEnd:true,subscriptionMonths:true,subscriptionType:true,analysisLimit:true,analysisLimit24h:true}});if(!u)return res.status(404).json({success:false,message:'کاربر یافت نشد'});const days=remainingDays(u.subscriptionStart,u.subscriptionEnd,u.subscriptionMonths);return res.json({success:true,data:{subscriptionStart:u.subscriptionStart,subscriptionEnd:u.subscriptionEnd,subscriptionDays:subscriptionDurationDays(u.subscriptionStart,u.subscriptionEnd,u.subscriptionMonths),subscriptionMonths:u.subscriptionMonths||0,subscriptionType:u.subscriptionType||'free',analysisLimit:u.analysisLimit24h||u.analysisLimit||0,analysisLimit24h:u.analysisLimit24h||u.analysisLimit||0,remainingDays:days,isActive:days>0,isSubscriptionActive:days>0}});}catch(e){return res.status(500).json({success:false,message:'خطا در دریافت اطلاعات اشتراک'});}}
async function recoverPassword(req,res){try{const email=normalizeEmail(req.body?.email);if(!required(email))return res.status(400).json({success:false,message:'ایمیل الزامی است'});const u=await prisma.user.findFirst({where:{OR:[{email},{username:email}],isDeleted:false},select:{id:true,email:true,username:true,passwordEncrypted:true,passwordHash:true}});if(!u)return res.status(404).json({success:false,message:'شما قبلا با این ایمیل ثبت نام انجام نداده اید'});let password=null;try{password=decryptPassword(u.passwordEncrypted);}catch(_){}if(!password){password=generatePassword();await prisma.user.update({where:{id:u.id},data:{passwordHash:await bcrypt.hash(password,12),passwordEncrypted:encryptPassword(password),updatedAt:new Date()}});}await sendPasswordEmail(email,password,'بازیابی کلمه عبور - تحلیلگر هوشمند بورس رونیا');return res.json({success:true,message:'کلمه عبور به ایمیل شما ارسال گردید لطفا جهت مشاهده کلمه عبور ایمیل خود را بررسی نمایید',data:{emailSent:true}});}catch(e){console.error('[AUTH] recoverPassword',e);return res.status(502).json({success:false,message:'ارسال ایمیل انجام نشد. لطفا دوباره تلاش کنید'});}}
async function resetPassword(req,res){return res.status(400).json({success:false,message:'برای بازیابی کلمه عبور از گزینه ارسال کلمه عبور به ایمیل استفاده کنید'});}
async function refreshToken(req,res){try{const tok=req.body?.refreshToken||req.cookies?.refreshToken;if(!tok)return res.status(401).json({success:false,message:'توکن ارائه نشده'});const d=jwt.verify(tok,secret('refresh')),u=await prisma.user.findUnique({where:{id:parseId(d.userId||d.sub)},select:USER_SELECT});if(!u||!u.isActive)return res.status(401).json({success:false,message:'کاربر یافت نشد'});const t=tokens(u);setCookies(req,res,t.accessToken,t.refreshToken);return res.json({success:true,data:{user:formatUser(u),token:t.accessToken,accessToken:t.accessToken,refreshToken:t.refreshToken}});}catch(e){return res.status(401).json({success:false,message:'توکن نامعتبر یا منقضی'});}}
async function logout(req,res){clearCookies(req,res);return res.json({success:true,message:'خروج موفق'});}
module.exports={login,register,verify,refreshToken,logout,me,updateProfile,changePassword,getSubscription,recoverPassword,resetPassword,refresh:refreshToken,getMe:me,getProfile:me};
