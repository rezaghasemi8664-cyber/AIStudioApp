import React, { useEffect, useState } from 'react';
import * as adminActionsService from '../services/adminActionsService';
import type { AdminModuleKey } from '../services/adminControlService';
import AdminSecuritySettingsPanels from './AdminSecuritySettingsPanels';

interface Props { moduleKey: AdminModuleKey; onComplete: () => Promise<void> | void; }
interface PaymentRow { id:number; userId:number; amount:number; currency:string; gateway?:string|null; referenceNo?:string|null; status:string; description?:string|null; createdAt?:string; paidAt?:string|null; }
interface BackupRow { id:number; type:string; filePath?:string|null; status:string; startedAt?:string; finishedAt?:string|null; sizeBytes?:number|null; errorMessage?:string|null; }
interface DeploymentRow { id:number; version?:string|null; channel:string; commitSha?:string|null; status:string; createdAt?:string; }
interface RoleRow { id:number; name:string; title?:string|null; userCount:number; permissionCount:number; }
interface SessionRow { id:number; userId:number; createdAt?:string|null; }
interface ApiKeyRow { id:number; userId:number; name?:string|null; createdAt?:string|null; isRevoked:boolean; }
const card='rounded-2xl border border-[var(--card-border-color)] bg-[var(--card-bg)] p-5';
const input='w-full rounded-xl border border-[var(--card-border-color)] bg-transparent px-3 py-2.5 text-sm outline-none focus:ring-2 focus:ring-cyan-500/30';
const statusLabel:Record<string,string>={pending:'در انتظار',paid:'پرداخت‌شده',failed:'ناموفق',cancelled:'لغوشده',refunded:'مستردشده',running:'در حال اجرا',completed:'تکمیل‌شده',success:'موفق',beta:'آزمایشی',stable:'پایدار'};
const fmt=(v:number)=>Number(v||0).toLocaleString('fa-IR');
const dateFmt=(v?:string|null)=>v?new Date(v).toLocaleString('fa-IR'):'—';
const badge=(value:string)=><span className="rounded-full bg-slate-100 px-2.5 py-1 text-xs dark:bg-slate-800">{statusLabel[value]||value}</span>;
const OperationalPanels:React.FC<Props>=({moduleKey,onComplete})=>{
 const [busy,setBusy]=useState(false); const [message,setMessage]=useState<string|null>(null); const [error,setError]=useState<string|null>(null);
 const [userId,setUserId]=useState(''); const [amount,setAmount]=useState(''); const [currency,setCurrency]=useState('IRR'); const [gateway,setGateway]=useState(''); const [description,setDescription]=useState('');
 const [transactionId,setTransactionId]=useState(''); const [status,setStatus]=useState('paid'); const [referenceNo,setReferenceNo]=useState(''); const [payments,setPayments]=useState<PaymentRow[]>([]);
 const [channel,setChannel]=useState('stable'); const [version,setVersion]=useState(''); const [commitSha,setCommitSha]=useState(''); const [deploymentStatus,setDeploymentStatus]=useState('success'); const [deployments,setDeployments]=useState<DeploymentRow[]>([]);
 const [backups,setBackups]=useState<BackupRow[]>([]); const [roles,setRoles]=useState<RoleRow[]>([]); const [sessions,setSessions]=useState<SessionRow[]>([]); const [apiKeys,setApiKeys]=useState<ApiKeyRow[]>([]);
 const [accessUserId,setAccessUserId]=useState(''); const [roleId,setRoleId]=useState(''); const [revokeId,setRevokeId]=useState('');
 const run=async(action:string,payload:Record<string,unknown>={})=>{setBusy(true);setMessage(null);setError(null);try{const result=await adminActionsService.executeAction(moduleKey,action,payload);if(moduleKey==='payments'&&action==='list-transactions')setPayments(Array.isArray(result)?result as PaymentRow[]:[]);if(moduleKey==='backup'&&action==='get-status')setBackups(Array.isArray(result)?result as BackupRow[]:[]);if(moduleKey==='updates'&&action==='get-deployments')setDeployments(Array.isArray(result)?result as DeploymentRow[]:[]);if(moduleKey==='roles'&&action==='list-roles')setRoles(Array.isArray(result)?result as RoleRow[]:[]);if(moduleKey==='sessions'&&action==='list-sessions')setSessions(Array.isArray(result)?result as SessionRow[]:[]);if(moduleKey==='api'&&action==='list-api-keys')setApiKeys(Array.isArray(result)?result as ApiKeyRow[]:[]);setMessage('عملیات با موفقیت انجام شد و در Audit Log ثبت شد.');await onComplete();}catch(e){setError(e instanceof Error?e.message:'اجرای عملیات ناموفق بود.');}finally{setBusy(false);}};
 useEffect(()=>{if(moduleKey==='payments')void run('list-transactions');if(moduleKey==='backup')void run('get-status');if(moduleKey==='updates')void run('get-deployments');if(moduleKey==='roles')void run('list-roles');if(moduleKey==='sessions')void run('list-sessions');if(moduleKey==='api')void run('list-api-keys');},[moduleKey]);
 if(moduleKey==='security'||moduleKey==='settings'||moduleKey==='maintenance') return <AdminSecuritySettingsPanels moduleKey={moduleKey} onComplete={onComplete}/>;
 if(moduleKey==='roles')return <div className={card} dir="rtl"><div className="mb-5"><h3 className="text-lg font-bold">نقش‌ها و مجوزها</h3><p className="mt-1 text-sm text-gray-500">مدیریت نقش‌های موجود و تخصیص نقش به کاربران.</p></div><div className="grid grid-cols-1 md:grid-cols-3 gap-3"><input className={input} value={accessUserId} onChange={e=>setAccessUserId(e.target.value)} placeholder="شناسه کاربر" dir="ltr"/><select className={input} value={roleId} onChange={e=>setRoleId(e.target.value)}><option value="">انتخاب نقش</option>{roles.map(r=><option key={r.id} value={r.id}>{r.title||r.name} — {r.userCount} کاربر</option>)}</select><button disabled={busy||!accessUserId||!roleId} onClick={()=>void run('assign-role',{userId:Number(accessUserId),roleId:Number(roleId)})} className="rounded-xl bg-cyan-600 px-5 py-2.5 font-semibold text-white disabled:opacity-50">تخصیص نقش</button></div><div className="mt-6 overflow-auto"><table className="w-full min-w-[620px] text-sm"><thead><tr className="border-b"><th className="p-3 text-right">نقش</th><th className="p-3 text-right">نام فنی</th><th className="p-3 text-right">کاربران</th><th className="p-3 text-right">مجوزها</th></tr></thead><tbody>{roles.map(r=><tr key={r.id} className="border-b"><td className="p-3">{r.title||'—'}</td><td className="p-3" dir="ltr">{r.name}</td><td className="p-3">{fmt(r.userCount)}</td><td className="p-3">{fmt(r.permissionCount)}</td></tr>)}</tbody></table>{roles.length===0&&<div className="py-6 text-center text-gray-500">نقشی ثبت نشده است.</div>}</div>{message&&<p className="mt-4 text-sm text-green-600">{message}</p>}{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}</div>;
 if(moduleKey==='sessions')return <div className={card} dir="rtl"><div className="mb-5"><h3 className="text-lg font-bold">مدیریت نشست‌های کاربران</h3><p className="mt-1 text-sm text-gray-500">Token نشست هرگز در پنل نمایش داده نمی‌شود.</p></div><div className="grid grid-cols-1 md:grid-cols-3 gap-3"><input className={input} value={revokeId} onChange={e=>setRevokeId(e.target.value)} placeholder="شناسه نشست" dir="ltr"/><button disabled={busy||!revokeId} onClick={()=>void run('revoke-session',{sessionId:Number(revokeId)})} className="rounded-xl border border-red-500/50 px-5 py-2.5 font-semibold text-red-600 disabled:opacity-50">لغو نشست</button><button disabled={busy||!accessUserId} onClick={()=>void run('revoke-all-user-sessions',{userId:Number(accessUserId)})} className="rounded-xl border border-amber-500/50 px-5 py-2.5 font-semibold text-amber-600 disabled:opacity-50">لغو همه نشست‌های کاربر</button></div><div className="mt-6 flex items-center justify-between"><h4 className="font-bold">نشست‌های اخیر</h4><button disabled={busy} onClick={()=>void run('list-sessions')} className="rounded-lg border px-3 py-2 text-sm">به‌روزرسانی</button></div><div className="mt-3 overflow-auto"><table className="w-full min-w-[520px] text-sm"><thead><tr className="border-b"><th className="p-3 text-right">شناسه نشست</th><th className="p-3 text-right">شناسه کاربر</th><th className="p-3 text-right">زمان ایجاد</th></tr></thead><tbody>{sessions.map(r=><tr key={r.id} className="border-b"><td className="p-3" dir="ltr">{r.id}</td><td className="p-3" dir="ltr">{r.userId}</td><td className="p-3">{dateFmt(r.createdAt)}</td></tr>)}</tbody></table>{sessions.length===0&&<div className="py-6 text-center text-gray-500">نشست فعالی پیدا نشد.</div>}</div>{message&&<p className="mt-4 text-sm text-green-600">{message}</p>}{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}</div>;
 if(moduleKey==='api')return <div className={card} dir="rtl"><div className="mb-5"><h3 className="text-lg font-bold">مدیریت API Key</h3><p className="mt-1 text-sm text-gray-500">برای امنیت، مقدار واقعی کلیدها از API مدیریتی برگردانده نمی‌شود.</p></div><div className="grid grid-cols-1 md:grid-cols-2 gap-3"><input className={input} value={revokeId} onChange={e=>setRevokeId(e.target.value)} placeholder="شناسه API Key برای لغو" dir="ltr"/><button disabled={busy||!revokeId} onClick={()=>void run('revoke-api-key',{apiKeyId:Number(revokeId)})} className="rounded-xl border border-red-500/50 px-5 py-2.5 font-semibold text-red-600 disabled:opacity-50">لغو کلید</button></div><div className="mt-6 flex items-center justify-between"><h4 className="font-bold">کلیدهای ثبت‌شده</h4><button disabled={busy} onClick={()=>void run('list-api-keys')} className="rounded-lg border px-3 py-2 text-sm">به‌روزرسانی</button></div><div className="mt-3 overflow-auto"><table className="w-full min-w-[620px] text-sm"><thead><tr className="border-b"><th className="p-3 text-right">شناسه</th><th className="p-3 text-right">کاربر</th><th className="p-3 text-right">نام کلید</th><th className="p-3 text-right">تاریخ ایجاد</th><th className="p-3 text-right">وضعیت</th></tr></thead><tbody>{apiKeys.map(r=><tr key={r.id} className="border-b"><td className="p-3" dir="ltr">{r.id}</td><td className="p-3" dir="ltr">{r.userId}</td><td className="p-3">{r.name||'بدون نام'}</td><td className="p-3">{dateFmt(r.createdAt)}</td><td className="p-3">{r.isRevoked?<span className="text-red-600">لغوشده</span>:<span className="text-green-600">فعال</span>}</td></tr>)}</tbody></table>{apiKeys.length===0&&<div className="py-6 text-center text-gray-500">API Key ثبت نشده است.</div>}</div>{message&&<p className="mt-4 text-sm text-green-600">{message}</p>}{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}</div>;
 if(moduleKey==='payments')return <div className={card} dir="rtl"><div className="mb-5"><h3 className="font-bold text-lg">مدیریت پرداخت و تراکنش</h3><p className="mt-1 text-sm text-gray-500">ثبت تراکنش و کنترل وضعیت، همراه با تاریخچه مالی قابل مشاهده.</p></div><div className="grid grid-cols-1 md:grid-cols-2 gap-4"><label className="space-y-1.5"><span className="text-sm font-medium">شناسه کاربر</span><input className={input} value={userId} onChange={e=>setUserId(e.target.value)} placeholder="مثلاً 125" dir="ltr"/></label><label className="space-y-1.5"><span className="text-sm font-medium">مبلغ</span><input className={input} value={amount} onChange={e=>setAmount(e.target.value)} type="number" min="0" placeholder="مثلاً 500000" dir="ltr"/></label><label className="space-y-1.5"><span className="text-sm font-medium">واحد پول</span><select className={input} value={currency} onChange={e=>setCurrency(e.target.value)}><option value="IRR">ریال</option><option value="IRT">تومان</option></select></label><label className="space-y-1.5"><span className="text-sm font-medium">درگاه</span><input className={input} value={gateway} onChange={e=>setGateway(e.target.value)} placeholder="نام درگاه"/></label><label className="space-y-1.5 md:col-span-2"><span className="text-sm font-medium">توضیحات</span><input className={input} value={description} onChange={e=>setDescription(e.target.value)} placeholder="توضیحات تراکنش"/></label></div><button disabled={busy||!userId||!amount} onClick={()=>void run('create-transaction',{userId:Number(userId),amount:Number(amount),currency,gateway,description})} className="mt-4 rounded-xl bg-cyan-600 px-5 py-2.5 font-semibold text-white disabled:opacity-50">ثبت تراکنش</button><div className="mt-6 border-t border-[var(--card-border-color)] pt-5"><h4 className="font-bold mb-3">تغییر وضعیت تراکنش</h4><div className="grid grid-cols-1 md:grid-cols-3 gap-3"><input className={input} value={transactionId} onChange={e=>setTransactionId(e.target.value)} placeholder="شناسه تراکنش" dir="ltr"/><select className={input} value={status} onChange={e=>setStatus(e.target.value)}><option value="pending">در انتظار</option><option value="paid">پرداخت‌شده</option><option value="failed">ناموفق</option><option value="cancelled">لغوشده</option><option value="refunded">مستردشده</option></select><input className={input} value={referenceNo} onChange={e=>setReferenceNo(e.target.value)} placeholder="شماره مرجع" dir="ltr"/></div><button disabled={busy||!transactionId} onClick={()=>void run('set-status',{transactionId,status,referenceNo})} className="mt-3 rounded-xl border border-cyan-500/50 px-5 py-2.5 font-semibold disabled:opacity-50">ثبت وضعیت</button></div><div className="mt-6 flex items-center justify-between"><h4 className="font-bold">آخرین تراکنش‌ها</h4><button disabled={busy} onClick={()=>void run('list-transactions')} className="rounded-lg border px-3 py-2 text-sm">به‌روزرسانی</button></div><div className="mt-3 overflow-auto"><table className="w-full min-w-[760px] text-sm"><thead><tr className="border-b"><th className="p-3 text-right">شناسه</th><th className="p-3 text-right">کاربر</th><th className="p-3 text-right">مبلغ</th><th className="p-3 text-right">درگاه</th><th className="p-3 text-right">وضعیت</th><th className="p-3 text-right">مرجع</th><th className="p-3 text-right">زمان</th></tr></thead><tbody>{payments.map(r=><tr key={r.id} className="border-b"><td className="p-3" dir="ltr">{r.id}</td><td className="p-3" dir="ltr">{r.userId}</td><td className="p-3">{fmt(r.amount)} {r.currency==='IRT'?'تومان':'ریال'}</td><td className="p-3">{r.gateway||'—'}</td><td className="p-3">{badge(r.status)}</td><td className="p-3" dir="ltr">{r.referenceNo||'—'}</td><td className="p-3">{dateFmt(r.createdAt)}</td></tr>)}</tbody></table>{payments.length===0&&<div className="py-6 text-center text-gray-500">تراکنشی ثبت نشده است.</div>}</div>{message&&<p className="mt-4 text-sm text-green-600">{message}</p>}{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}</div>;
 if(moduleKey==='backup')return <div className={card} dir="rtl"><div className="mb-5"><h3 className="text-lg font-bold">پشتیبان‌گیری و بازیابی</h3><p className="mt-1 text-sm text-gray-500">ایجاد Backup واقعی پایگاه داده و نمایش سوابق عملیات.</p></div><div className="flex flex-wrap gap-3"><button disabled={busy} onClick={()=>void run('create-backup',{type:'database'})} className="rounded-xl bg-cyan-600 px-5 py-2.5 font-semibold text-white disabled:opacity-50">ایجاد پشتیبان جدید</button><button disabled={busy} onClick={()=>void run('get-status')} className="rounded-xl border px-5 py-2.5 font-semibold disabled:opacity-50">به‌روزرسانی سوابق</button></div><div className="mt-5 overflow-auto"><table className="w-full min-w-[760px] text-sm"><thead><tr className="border-b"><th className="p-3 text-right">شناسه</th><th className="p-3 text-right">نوع</th><th className="p-3 text-right">وضعیت</th><th className="p-3 text-right">حجم</th><th className="p-3 text-right">شروع</th><th className="p-3 text-right">پایان</th><th className="p-3 text-right">خطا</th></tr></thead><tbody>{backups.map(r=><tr key={r.id} className="border-b"><td className="p-3" dir="ltr">{r.id}</td><td className="p-3">پایگاه داده</td><td className="p-3">{badge(r.status)}</td><td className="p-3">{r.sizeBytes?`${fmt(Math.round(r.sizeBytes/1024/1024))} مگابایت`:'—'}</td><td className="p-3">{dateFmt(r.startedAt)}</td><td className="p-3">{dateFmt(r.finishedAt)}</td><td className="p-3 text-red-600 max-w-[260px]">{r.errorMessage||'—'}</td></tr>)}</tbody></table>{backups.length===0&&<div className="py-6 text-center text-gray-500">هنوز عملیات پشتیبان‌گیری ثبت نشده است.</div>}</div><div className="mt-5 rounded-xl bg-amber-50 dark:bg-amber-950/20 p-4 text-sm text-amber-800 dark:text-amber-200">برای Backup واقعی باید <span dir="ltr">ADMIN_BACKUP_DIR</span> روی سرور تنظیم شده باشد و حساب سرویس SQL Server مجوز نوشتن در آن مسیر را داشته باشد.</div>{message&&<p className="mt-4 text-sm text-green-600">{message}</p>}{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}</div>;
 if(moduleKey==='updates')return <div className={card} dir="rtl"><div className="mb-5"><h3 className="font-bold text-lg">بروزرسانی و استقرار</h3><p className="mt-1 text-sm text-gray-500">انتخاب کانال انتشار، ثبت استقرار و مشاهده تاریخچه انتشار.</p></div><div className="grid grid-cols-1 md:grid-cols-2 gap-4"><label className="space-y-1.5"><span className="text-sm font-medium">کانال انتشار</span><select className={input} value={channel} onChange={e=>setChannel(e.target.value)}><option value="stable">پایدار</option><option value="beta">آزمایشی</option></select></label><label className="space-y-1.5"><span className="text-sm font-medium">نسخه</span><input className={input} value={version} onChange={e=>setVersion(e.target.value)} placeholder="مثلاً 2.2.0" dir="ltr"/></label><label className="space-y-1.5"><span className="text-sm font-medium">Commit SHA</span><input className={input} value={commitSha} onChange={e=>setCommitSha(e.target.value)} placeholder="SHA استقرار" dir="ltr"/></label><label className="space-y-1.5"><span className="text-sm font-medium">نتیجه استقرار</span><select className={input} value={deploymentStatus} onChange={e=>setDeploymentStatus(e.target.value)}><option value="success">موفق</option><option value="failed">ناموفق</option><option value="pending">در انتظار</option></select></label></div><div className="mt-4 flex flex-wrap gap-2"><button disabled={busy} onClick={()=>void run('set-channel',{channel})} className="rounded-xl border border-cyan-500/50 px-5 py-2.5 font-semibold disabled:opacity-50">ذخیره کانال انتشار</button><button disabled={busy||(!version&&!commitSha)} onClick={()=>void run('record-deployment',{version,channel,commitSha,status:deploymentStatus})} className="rounded-xl bg-cyan-600 px-5 py-2.5 font-semibold text-white disabled:opacity-50">ثبت استقرار</button></div><div className="mt-6 flex items-center justify-between"><h4 className="font-bold">تاریخچه استقرار</h4><button disabled={busy} onClick={()=>void run('get-deployments')} className="rounded-lg border px-3 py-2 text-sm">به‌روزرسانی</button></div><div className="mt-3 overflow-auto"><table className="w-full min-w-[760px] text-sm"><thead><tr className="border-b"><th className="p-3 text-right">نسخه</th><th className="p-3 text-right">کانال</th><th className="p-3 text-right">Commit</th><th className="p-3 text-right">وضعیت</th><th className="p-3 text-right">زمان</th></tr></thead><tbody>{deployments.map(r=><tr key={r.id} className="border-b"><td className="p-3" dir="ltr">{r.version||'—'}</td><td className="p-3">{statusLabel[r.channel]||r.channel}</td><td className="p-3 max-w-[280px] truncate" dir="ltr">{r.commitSha||'—'}</td><td className="p-3">{badge(r.status)}</td><td className="p-3">{dateFmt(r.createdAt)}</td></tr>)}</tbody></table>{deployments.length===0&&<div className="py-6 text-center text-gray-500">هنوز استقراری ثبت نشده است.</div>}</div><div className="mt-5 rounded-xl bg-slate-50 dark:bg-slate-900/50 p-4 text-sm text-gray-600 dark:text-gray-300">این بخش نتیجه استقرار را ثبت می‌کند؛ اجرای واقعی Deploy همچنان توسط CI/CD انجام می‌شود.</div>{message&&<p className="mt-4 text-sm text-green-600">{message}</p>}{error&&<p className="mt-4 text-sm text-red-600">{error}</p>}</div>;
 return null;
};
export default OperationalPanels;
