import React,{useCallback,useEffect,useState}from'react';
import*as svc from'../services/adminActionsService';
interface Props{onComplete?:()=>Promise<void>|void}
type Service={name:string;status:'healthy'|'degraded'|'down'|'unknown';latencyMs?:number|null;httpStatus?:number;message?:string;uptimeSeconds?:number};type Health={status?:string;checkedAt?:string;node?:Service;memory?:{rss:number;heapUsed:number;heapTotal:number};services?:Service[]};
const mb=(v:number|undefined)=>Math.round((v||0)/1024/1024).toLocaleString('fa-IR');const labels:Record<string,string>={database:'پایگاه داده',node:'Node.js',tsetmc:'TSETMC',codal:'Codal',ai:'هوش مصنوعی'};const statusLabel=(s?:string)=>s==='healthy'?'سالم':s==='degraded'?'اختلال':s==='down'?'قطع':'نامشخص';const statusClass=(s?:string)=>s==='healthy'?'border-emerald-200 bg-emerald-50 text-emerald-700':s==='degraded'?'border-amber-200 bg-amber-50 text-amber-700':s==='down'?'border-red-200 bg-red-50 text-red-700':'border-gray-200 bg-gray-50 text-gray-600';
const AdminMonitoringPanel:React.FC<Props>=({onComplete=()=>undefined})=>{const[h,setH]=useState<Health|null>(null),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null);const load=useCallback(async()=>{setLoading(true);setError(null);try{const x=await svc.executeAction<Health>('monitoring','health-check',{});setH(x);await onComplete()}catch(e){setError(e instanceof Error?e.message:'بررسی سلامت سامانه ناموفق بود.')}finally{setLoading(false)}},[onComplete]);useEffect(()=>{void load()},[load]);const services=h?.services||[];const node=h?.node;return <div dir="rtl" className="space-y-5"><div className="rounded-2xl border border-[var(--card-border-color)] bg-[var(--card-bg)] p-5"><div className="flex items-center justify-between gap-3"><div><h3 className="text-lg font-bold">پایش و سلامت سامانه</h3><p className="mt-1 text-sm text-gray-500">وضعیت لحظه‌ای سرویس‌ها، پایگاه داده و منابع پردازشی</p></div><div className="flex items-center gap-2"><span className={`rounded-full border px-3 py-1 text-xs font-semibold ${statusClass(h?.status)}`}>{statusLabel(h?.status)}</span><button onClick={()=>void load()} disabled={loading} className="rounded-xl border px-4 py-2 text-sm disabled:opacity-50">{loading?'در حال بررسی...':'بررسی مجدد'}</button></div></div></div>{error&&<div className="rounded-xl border border-red-200 bg-red-50 p-4 text-sm text-red-700">{error}</div>}<div className="grid grid-cols-1 md:grid-cols-2 xl:grid-cols-5 gap-3">{services.map(s=><div key={s.name} className="rounded-2xl border bg-[var(--card-bg)] p-4"><div className="flex items-center justify-between"><b>{labels[s.name]||s.name}</b><span className={`rounded-full border px-2 py-1 text-xs ${statusClass(s.status)}`}>{statusLabel(s.status)}</span></div><div className="mt-4 text-sm text-gray-500">زمان پاسخ: <b className="text-gray-800 dark:text-gray-200">{s.latencyMs==null?'—':s.latencyMs.toLocaleString('fa-IR')+' ms'}</b></div>{s.httpStatus&&<div className="mt-1 text-xs text-gray-500">HTTP: {s.httpStatus}</div>}{s.message&&<div className="mt-2 text-xs text-red-600 break-words">{s.message}</div>}</div>)}</div><div className="grid grid-cols-1 md:grid-cols-3 gap-3"><div className="rounded-2xl border bg-[var(--card-bg)] p-4"><div className="text-xs text-gray-500">Uptime</div><b className="mt-2 block">{Math.floor((node?.uptimeSeconds||0)/3600).toLocaleString('fa-IR')} ساعت و {Math.floor(((node?.uptimeSeconds||0)%3600)/60).toLocaleString('fa-IR')} دقیقه</b></div><div className="rounded-2xl border bg-[var(--card-bg)] p-4"><div className="text-xs text-gray-500">RAM</div><b className="mt-2 block">{mb(h?.memory?.rss)} MB</b><span className="text-xs text-gray-500">Heap: {mb(h?.memory?.heapUsed)} / {mb(h?.memory?.heapTotal)} MB</span></div><div className="rounded-2xl border bg-[var(--card-bg)] p-4"><div className="text-xs text-gray-500">آخرین بررسی</div><b className="mt-2 block">{h?.checkedAt?new Date(h.checkedAt).toLocaleString('fa-IR'):'—'}</b></div></div></div>};export default AdminMonitoringPanel;
