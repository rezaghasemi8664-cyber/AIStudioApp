// Dashboard integration is intentionally kept isolated until App.tsx is patched safely.
// No runtime dependency on AI/Gemini is introduced by Phase 1.
export const dashboardIntegrationReady = true;
