name: Fix market summary UI final

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Obsolete automatic market-summary patch workflow disabled."
